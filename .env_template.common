# Common settings shared by all crawlers
# Database connection
DB_HOST=db
DB_PORT=5432
DB_NAME=armiarmadb
DB_USER=user
DB_PASSWORD=password

# Default PostgreSQL endpoint (can be overridden per crawler)
CRAWLER_PSQL_ENDP=postgresql://user:password@db:5432/armiarmadb

